# /context-plan

Plan which files should be loaded for a task.

Example:

```text
/context-plan AAPL --task full_research --mode L1
```

Output:

- Context packet
- Estimated token budget
- Files loaded now
- Files deferred
- Reasoning for exclusions
