# /mindmap

Generate a mind-map-style summary for the current stock analysis.

Output:

- Nested bullet mind map
- Optional ASCII tree
- Optional Mermaid mindmap block
- Key decision path
- Most important next monitoring points

Example:

```text
/mindmap GOOGL
/mindmap TSLA --format mermaid
/mindmap JPM --format tree
```
